const config = {
  translations:{
    en:{
      "app.components.LeftMenu.navbrand.title": "Suba Vote Project CMS",
      "Auth.form.welcome.title": "Welcome to Suba Vote Project CMS",
      "Auth.form.welcome.subtitle": "Log in to your Suba Vote account",
    }
  }
};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
